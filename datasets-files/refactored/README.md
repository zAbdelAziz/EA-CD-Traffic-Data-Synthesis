# Refactored data cache

Generated real-data windows and metadata are written here by `NgsimDataset(raw=True)`.

Typical generated files:

```text
ngsim.csv
ngsim.npz
ngsim.std.npz
```

These files are ignored by git.
